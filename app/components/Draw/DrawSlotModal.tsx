"use client";

import type { DrawPlayer, DrawSlot } from "../shared/types";

export function DrawSlotModal({ slot, players, assignedIds, search, setSearch, selectPlayer, selectBye, clear, close }: { slot: DrawSlot; players: DrawPlayer[]; assignedIds: Set<number>; search: string; setSearch: (value: string) => void; selectPlayer: (player: DrawPlayer) => void; selectBye: () => void; clear: () => void; close: () => void }) {
  const available = players.filter(player => (!assignedIds.has(player.id) || player.id === slot.player_id) && `${player.name} ${player.email} ${player.sponsor_name ?? ""}`.toLowerCase().includes(search.toLowerCase()));
  return <div className="modal-backdrop" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) close(); }}><div className="modal-card draw-picker" role="dialog" aria-modal="true" aria-labelledby="draw-picker-title"><div className="modal-header"><div><p>POSITIE {slot.position}</p><h2 id="draw-picker-title">Speler of bye kiezen</h2></div><button type="button" className="modal-close" onClick={close} aria-label="Sluiten">×</button></div><div className="draw-picker-actions"><button className="draw-bye-option" type="button" onClick={selectBye}><span>BYE</span><div><strong>Vrijstelling plaatsen</strong><small>Deze positie bevat geen speler.</small></div></button>{(slot.player_id || slot.is_bye) && <button className="secondary" type="button" onClick={clear}>Positie leegmaken</button>}</div><div className="search draw-picker-search">⌕ <input autoFocus value={search} onChange={event => setSearch(event.target.value)} placeholder="Zoek op naam, e-mail of sponsor" /></div><div className="draw-player-options">{available.length === 0 ? <div className="draw-no-results">Geen beschikbare spelers gevonden.</div> : available.map(player => <button type="button" key={player.id} onClick={() => selectPlayer(player)}><span>{player.player_number ?? player.name.split(" ").map(part => part[0]).slice(0, 2).join("")}</span><div><strong>{player.name}</strong><small>{player.email}{player.sponsor_name ? ` · ${player.sponsor_name}` : ""}</small></div>{player.id === slot.player_id && <b>Huidig</b>}</button>)}</div></div></div>;
}
